apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    JAVA_API_DEPENDENCIES.each {
        add(it.configuration, it.dependency)
    }
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
