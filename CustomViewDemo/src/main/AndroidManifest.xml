<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.luckyzhangx.customviewdemo"
    tools:ignore="GoogleAppIndexingWarning"
    >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".gridimg.GridImgActivity">


        </activity>
        <activity android:name=".CustomViewDemoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".HorizontalPullDemoActivity" />
        <activity android:name=".CustomViewDemoActivity" />
        <activity android:name=".textview.CustomTextViewDemoActivity" />
        <activity android:name=".PullBounceDemoActivity" />
    </application>

</manifest>